// ===========================================================================//	LSimpleThread.cp				©1994 Metrowerks Inc. All rights reserved.////									Author:  Paul Lalonde// ===========================================================================#ifdef PowerPlant_PCH#include PowerPlant_PCH#endif// PowerPlant headers#include <LSimpleThread.h>// ===========================================================================//	¥ Member functions// ===========================================================================// ---------------------------------------------------------------------------//		¥ LSimpleThread// ---------------------------------------------------------------------------//	Constructor.  Initialises a LSimpleThread object.LSimpleThread::LSimpleThread(	ThreadProc				proc, 	void					*arg, 	Boolean					preempt, 	Uint32					stack, 	LThread::EThreadOption	flags)		// call base class's constructor	: LThread(preempt, stack, flags){	THREAD_ASSERT(proc != NULL);		mProc	= proc;	mArg	= arg;}// ---------------------------------------------------------------------------//		¥ Run// ---------------------------------------------------------------------------//	Execute the code for this thread.void	*LSimpleThread::Run(void){	(*mProc)(*this, mArg);		return (mResult);}