// ===========================================================================//	LListener.h						©1993 Metrowerks Inc. All rights reserved.// ===========================================================================#pragma once#include <LList.h>class	LBroadcaster;class	LListener {	friend class LBroadcaster;public:					LListener();					LListener(const LListener &inOriginal);	virtual			~LListener();		void			StartListening();	void			StopListening();	Boolean			IsListening() const;												// Pure Virtual. Concrete subclasses must override	virtual void	ListenToMessage(MessageT inMessage, void *ioParam) = 0;protected:	LList			mBroadcasters;	Boolean			mIsListening;	void			AddBroadcaster(LBroadcaster *inBroadcaster);	void			RemoveBroadcaster(LBroadcaster *inBroadcaster);};