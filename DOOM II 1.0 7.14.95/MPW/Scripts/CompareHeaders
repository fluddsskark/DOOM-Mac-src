###			CompareHeaders		Dir1   Dir2##			Takes two directories, for each file ending in .h in dir1, it tries to compare that#           file with a file by the same name in dir2.#Set Exit 0Set CaseSensitive 0Set d1 ""Set d2 ""Set exitStatus 0 If {#} == 0	echo  "Specify directory 1"	Exit {exitStatus}Else 	Set d1 "{1}"	Shift 1EndIf {#} == 0	echo  "Specify directory 2"	Exit {exitStatus}Else 	Set d2 "{1}"EndSetDirectory "{d1}"For File In Å.c	Compare -b -t "{File}" "{d2}""{File}"	Echo "{File}"EndExit {exitStatus}